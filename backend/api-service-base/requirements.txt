# ================================================================================
# TAXI API - REQUIREMENTS
# ================================================================================

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database (PostgreSQL vía SQLAlchemy)
sqlalchemy==2.0.25
psycopg2-binary==2.9.9

# Validation & Serialization
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP Client (llamadas a MercadoPago, whatsapp-gateway, geocoding)
httpx==0.26.0

# Monitoring
prometheus-client==0.19.0

# Auth (JWT)
python-jose[cryptography]==3.3.0

# Formularios (UploadFile/File — fotos de conductores, audio de incidentes)
python-multipart==0.0.6

# QR codes (mesas / landing)
qrcode[pil]==7.4.2

# Tests
pytest==7.4.4
