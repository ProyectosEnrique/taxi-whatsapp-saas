# ============================================
# TAXI AGENT - REQUIREMENTS
# Único servicio vivo: src/api/app_v2.py (Flask) + src/taxi/agent.py
# (LLM function calling vía SDK de OpenAI, reusado para Groq/Cerebras/
# Gemini/OpenRouter cambiando solo base_url) + Redis para sesiones.
# ============================================

# Web Framework
Flask[async]==3.0.0
Flask-CORS==4.0.0

# HTTP Client (llamadas a taxi-api y a los proveedores LLM)
httpx==0.27.0

# LLM (Groq/Cerebras/Gemini/OpenRouter, todos vía SDK de OpenAI + base_url)
openai>=1.0.0

# Sesiones de conversación
redis>=5.0.0

# Servidor WSGI
gunicorn==21.2.0
