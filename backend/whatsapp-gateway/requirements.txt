# ================================================================================
# TAXI WHATSAPP GATEWAY - Dependencies
# Único servicio vivo: src/main_taxi.py (FastAPI) + src/stt_client.py
# ================================================================================

# Framework web
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Formularios (Form(...) del webhook de Twilio)
python-multipart==0.0.6

# Cliente HTTP async (llamadas a taxi-agent, Meta Graph API, descarga de audio)
httpx==0.25.2
aiohttp==3.9.1

# Twilio SDK (enviar mensajes de WhatsApp)
twilio==8.10.0
